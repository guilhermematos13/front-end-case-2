import { ReactNode } from 'react'

export const metadata = {
  title: 'Veiculos',
  description: 'Administrar dados de Veiculos',
}

export default function RootLayout({ children }: { children: ReactNode }) {
  return children
}
