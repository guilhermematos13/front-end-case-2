import { ReactNode } from 'react'

export const metadata = {
  title: 'Condutores',
  description: 'Administrar dados de condutores',
}

export default function RootLayout({ children }: { children: ReactNode }) {
  return children
}
